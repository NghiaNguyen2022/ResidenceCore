diff --git a/server/db/storeLedger.ts b/server/db/storeLedger.ts
--- a/server/db/storeLedger.ts
+++ b/server/db/storeLedger.ts
@@
 export async function getStoreDutyAccessSessionById(id: number) {
@@
 }
+
+export async function getStoreDutyAccessSessionByTokenHash(
+      accessTokenHash: string,
+) {
+      const db = await dbOrThrow();
+      const rows = await db
+            .select()
+            .from(storeDutyAccessSessions)
+            .where(eq(storeDutyAccessSessions.accessTokenHash, accessTokenHash))
+            .orderBy(desc(storeDutyAccessSessions.id))
+            .limit(1);
+      return rows[0] ?? null;
+}
diff --git a/server/routers/modules/residentPortal.ts b/server/routers/modules/residentPortal.ts
--- a/server/routers/modules/residentPortal.ts
+++ b/server/routers/modules/residentPortal.ts
@@
       verifyMyStoreAccessCode: protectedProcedure
@@
             }),
+
+      getMyStoreAccessSession: protectedProcedure
+            .input(
+                  z.object({
+                        storeShiftId: z.number().int().positive(),
+                        accessToken: z.string().trim().min(20),
+                  }),
+            )
+            .query(async ({ ctx, input }) => {
+                  const userId = getUserIdFromContext(ctx);
+                  return storeDutyAccessService.getMyActiveAccessSession({
+                        userId,
+                        storeShiftId: input.storeShiftId,
+                        accessToken: input.accessToken,
+                  });
+            }),
diff --git a/server/routers/modules/storeLedger.ts b/server/routers/modules/storeLedger.ts
--- a/server/routers/modules/storeLedger.ts
+++ b/server/routers/modules/storeLedger.ts
@@
 function requireStoreLedgerAccess(user: {
@@
 }
+
+const residentStoreAccessSchema = {
+      storeShiftId: z.number().int().positive().optional().nullable(),
+      storeAccessToken: z.string().trim().min(20).optional().nullable(),
+};
+
+async function requireStoreOperationAccess(input: {
+      ctx: any;
+      storeShiftId?: number | null;
+      storeAccessToken?: string | null;
+      ledgerId?: number | null;
+      touchActivity?: boolean;
+}) {
+      return storeDutyAccessService.authorizeStoreAction({
+            user: input.ctx.user,
+            storeShiftId: input.storeShiftId,
+            accessToken: input.storeAccessToken,
+            ledgerId: input.ledgerId,
+            touchActivity: input.touchActivity,
+      });
+}
@@
-                  z.object({
+                  z.object({
                         ledgerId: z.number().int().positive().optional().nullable(),
@@
-                  }).optional(),
+                        ...residentStoreAccessSchema,
+                  }).optional(),
@@
-                  requireStoreLedgerAccess(ctx.user);
+                  await requireStoreOperationAccess({
+                        ctx,
+                        storeShiftId: input?.storeShiftId,
+                        storeAccessToken: input?.storeAccessToken,
+                        ledgerId: input?.ledgerId,
+                  });
                   return storeLedgerService.listDocuments(input || {});
@@
-                  z.object({ id: z.number().int().positive() })
+                  z.object({
+                        id: z.number().int().positive(),
+                        ...residentStoreAccessSchema,
+                  })
@@
-                  requireStoreLedgerAccess(ctx.user);
+                  await requireStoreOperationAccess({
+                        ctx,
+                        storeShiftId: input.storeShiftId,
+                        storeAccessToken: input.storeAccessToken,
+                  });
                   return storeLedgerService.getDocument(input.id);
@@
                         lines: z.array(z.object({
@@
                         })).min(1),
+                        ...residentStoreAccessSchema,
                   }),
@@
-                  requireStoreLedgerAccess(ctx.user);
-                  return storeLedgerService.createStockInDocument({ ...input, createdBy: getUserId(ctx) });
+                  const access = await requireStoreOperationAccess({
+                        ctx,
+                        storeShiftId: input.storeShiftId,
+                        storeAccessToken: input.storeAccessToken,
+                        ledgerId: input.ledgerId,
+                  });
+                  const { storeShiftId, storeAccessToken, ...documentInput } = input;
+                  return storeLedgerService.createStockInDocument({
+                        ...documentInput,
+                        createdBy: getUserId(ctx),
+                        storeShiftId: access.storeShiftId,
+                        storeDutyAssignmentId: access.storeDutyAssignmentId,
+                        createdByResidentId: access.residentId,
+                  } as any);
@@
                         lines: z.array(z.object({
@@
                         })).min(1),
+                        ...residentStoreAccessSchema,
                   }),
@@
-                  requireStoreLedgerAccess(ctx.user);
-                  return storeLedgerService.createSaleDocument({ ...input, createdBy: getUserId(ctx) });
+                  const access = await requireStoreOperationAccess({
+                        ctx,
+                        storeShiftId: input.storeShiftId,
+                        storeAccessToken: input.storeAccessToken,
+                        ledgerId: input.ledgerId,
+                  });
+                  const { storeShiftId, storeAccessToken, ...documentInput } = input;
+                  return storeLedgerService.createSaleDocument({
+                        ...documentInput,
+                        createdBy: getUserId(ctx),
+                        storeShiftId: access.storeShiftId,
+                        storeDutyAssignmentId: access.storeDutyAssignmentId,
+                        createdByResidentId: access.residentId,
+                  } as any);
@@
-                  z.object({
+                  z.object({
                         search: z.string().optional().nullable(),
@@
-                  }).optional(),
+                        ...residentStoreAccessSchema,
+                  }).optional(),
@@
-                  requireStoreLedgerAccess(ctx.user);
+                  await requireStoreOperationAccess({
+                        ctx,
+                        storeShiftId: input?.storeShiftId,
+                        storeAccessToken: input?.storeAccessToken,
+                  });
                   return storeLedgerService.listProducts(input || { isActive: true });
@@
-                  z.object({
+                  z.object({
@@
-                  }).optional(),
+                        ...residentStoreAccessSchema,
+                  }).optional(),
@@
-                  requireStoreLedgerAccess(ctx.user);
+                  await requireStoreOperationAccess({
+                        ctx,
+                        storeShiftId: input?.storeShiftId,
+                        storeAccessToken: input?.storeAccessToken,
+                  });
                   return storeLedgerService.listStockMovements(input || {});
@@
-                  z.object({
+                  z.object({
                         ledgerId: z.number().int().positive().optional().nullable(),
@@
-                  }).optional(),
+                        ...residentStoreAccessSchema,
+                  }).optional(),
@@
-                  requireStoreLedgerAccess(ctx.user);
+                  await requireStoreOperationAccess({
+                        ctx,
+                        storeShiftId: input?.storeShiftId,
+                        storeAccessToken: input?.storeAccessToken,
+                        ledgerId: input?.ledgerId,
+                  });
                   return storeLedgerService.getSummary(input || {});
@@
-                  z.object({
+                  z.object({
                         ledgerId: z.number().int().positive().optional().nullable(),
@@
-                  }).optional(),
+                        ...residentStoreAccessSchema,
+                  }).optional(),
@@
-                  requireStoreLedgerAccess(ctx.user);
+                  await requireStoreOperationAccess({
+                        ctx,
+                        storeShiftId: input?.storeShiftId,
+                        storeAccessToken: input?.storeAccessToken,
+                        ledgerId: input?.ledgerId,
+                  });
                   return storeLedgerService.listTransactions(input || {});
diff --git a/client/src/pages/MyDuties.tsx b/client/src/pages/MyDuties.tsx
--- a/client/src/pages/MyDuties.tsx
+++ b/client/src/pages/MyDuties.tsx
@@
                   onSuccess: (result) => {
@@
                         toast.success(result.message || "Đã mở quyền Cửa hàng.");
                   },
@@
             });
+
+      const activeStoreSession = useMemo(() => {
+            try {
+                  const raw = sessionStorage.getItem("residentStoreAccess");
+                  return raw ? JSON.parse(raw) : null;
+            } catch {
+                  return null;
+            }
+      }, [storeAccessOpen, verifyStoreAccessMutation.isSuccess]);
+
+      const activeStoreSessionQuery =
+            trpc.residentPortal.getMyStoreAccessSession.useQuery(
+                  {
+                        storeShiftId: Number(
+                              activeStoreSession?.storeShiftId || 0,
+                        ),
+                        accessToken: String(
+                              activeStoreSession?.accessToken || "",
+                        ),
+                  },
+                  {
+                        enabled: Boolean(
+                              activeStoreSession?.storeShiftId &&
+                                    activeStoreSession?.accessToken,
+                        ),
+                        retry: false,
+                        refetchOnWindowFocus: true,
+                  },
+            );
+
+      if (
+            activeStoreSessionQuery.data &&
+            !activeStoreSessionQuery.data.active
+      ) {
+            sessionStorage.removeItem("residentStoreAccess");
+      }
